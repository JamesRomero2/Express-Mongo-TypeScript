{
  "name": "expressmongots",
  "version": "1.0.0",
  "description": "",
  "main": "build/server.js",
  "scripts": {
    "build": "rm -rf build/ && npm install && tsc",
    "start": "node build/server.js",
    "deploy": "rm -rf build/ && tsc && node build/server.js"
  },
  "keywords": [],
  "author": "James Romero",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^17.8.4",
    "mongoose": "^7.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "typescript": "^4.9.5"
  }
}
